{
  "name": "vite-app",
  "version": "1.0.0",
  "private": true,
  "description": "Vite 8 app that reproduces the Windows path-separator bug when using @use with package exports `sass` condition",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vite-sass-bug/example": "workspace:*"
  },
  "devDependencies": {
    "sass-embedded": "^1.0.0",
    "vite": "^8.0.0"
  }
}
